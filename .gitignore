# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Env & secrets
.env

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Per-install auth signing secret (generated at first run)
backend/data/.auth_secret

# Node (flybasis-mcp)
node_modules/

# Captured HTTP archives may contain session tokens — never commit
*.har
